# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
env/
venv/
.venv/
*.egg-info/
dist/
build/
.eggs/
pip-wheel-metadata/
*.egg

# Virtual environments
.env
.env.*

# Test / coverage
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
*.cover
.tmp_source_spine_tests/

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Artifact bundles (generated, not committed)
*.zip
evidence/provenance/*/

# Validation reports (generated, not committed to main)
evidence/validation_reports/*.json
evidence/validation_reports/*.md
!evidence/validation_reports/scene_hydration_report.json
!evidence/validation_reports/canon_hydration_queue.json
!evidence/validation_reports/canon_hydration_queue.md

# devcontainer local overrides
.devcontainer/

# Claude Code local settings (machine-specific paths, not for repo)
.claude/settings.local.json

# Temporary PR body files
PR_BODY_*.md

# OS
.DS_Store
desktop.ini

# ── Batch 0.75: Production output binary protections ──────────────────────────
# Kling / video take binaries — external storage only; never committed to repo
visual_dev/omni_sets/**/takes/*.mp4
visual_dev/omni_sets/**/takes/*.mov

# Post-production proxy and render binaries — external storage only
post/edit/proxies/**/*.mp4
post/edit/proxies/**/*.mov
post/edit/proxies/**/*.mkv
post/edit/proxies/**/*.wav

# Storyboard candidate stills — external storage only; repo holds storyboard_options.yaml metadata
visual_dev/storyboards/**/candidates/

# Element wardrobe/prop model-specific candidate working folders — not canonical assets
visual_dev/elements/**/candidates_*/

# B8A canonical asset intake staging — local drop zone; files moved to elements/** via auto-placer
visual_dev/intake_staging/

# ── Local media archive (scripts/archive_media.py) ────────────────────────────
# Externally-produced image/video binaries filed into a standardized local tree by
# archive_media.py. LOCAL-ONLY: never pushed to the remote (repo would bloat). The
# committed record is the metadata-only local_media_index under evidence/.
archive/
# ──────────────────────────────────────────────────────────────────────────────
